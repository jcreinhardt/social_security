[pytest]
# Put code/ and code/lib/ on sys.path so tests import the library modules
# (msm_model, tiktak, ...) without installing anything. Run from code/: `pytest`.
pythonpath = . lib
testpaths = tests
markers =
    slow: end-to-end optimizer runs (deselect with -m "not slow")
