[pytest]
# Put code/ on sys.path so tests import the flat modules (msm_model, tiktak, ...)
# without installing anything. Run from the code/ directory: `pytest`.
pythonpath = .
testpaths = tests
markers =
    slow: end-to-end optimizer runs (deselect with -m "not slow")
